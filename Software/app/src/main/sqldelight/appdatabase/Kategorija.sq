CREATE TABLE kategorija (
    kategorijaID INTEGER NOT NULL PRIMARY KEY,
    naziv TEXT NOT NULL
);

insertKategorija:
INSERT INTO kategorija (kategorijaID, naziv)
VALUES (?, ?);

dohvatiNazivKategorije:
SELECT naziv FROM kategorija
WHERE kategorijaID = ?;