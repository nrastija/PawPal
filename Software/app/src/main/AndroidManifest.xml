<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:name=".main.PawPalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PawPal"
        tools:targetApi="31" >

        <activity
            android:name=".ui.PrijavaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="com.example.pawpal"
                    android:host="paypalpay" />
            </intent-filter>
        </activity>


        <activity android:name=".main.BaseActivity" />
        <activity android:name=".ui.CheckoutActivity" />
        <activity android:name=".f04_veterinar.odabirVeterinaraActivity"/>
        <activity android:name=".f04_veterinar.RezervacijaVeterinaraActivity"/>
        <activity android:name=".f04_veterinar.PotvrdaRezervacije"/>
        <activity android:name=".ui.RegistracijaActivity"></activity>

    </application>

</manifest>